import React from "react";
import StyledFormFooter from "../../styles/FormFooter.css.js";

const FormFooter = ({ body }) => {
  return <StyledFormFooter>{body}</StyledFormFooter>;
};

export default FormFooter;
